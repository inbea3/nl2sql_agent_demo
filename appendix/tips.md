对数据库模式的处理，是本地硬编码写好每次运行直接读取文件，还是写一个脚本每次运行的时候读取最新的模式？
由于数据库模式不应该是频繁变动的，所以本地硬编码应该是更好的解决方案，只需要定期修正更新，或者每次发生变动的时候对应更新即可。
若每次运行读取最新的模式，会冗余大量的内容，并且浪费很多时间。

数据库模式本身不常变动，本地硬编码或预定义配置文件是更优方案，性能高效、稳定可控，只需在结构变更时同步更新即可；而每次运行都读取最新模式会产生冗余开销、浪费资源，仅适用于需适配未知结构的通用工具类场景，常规业务无需这么做。